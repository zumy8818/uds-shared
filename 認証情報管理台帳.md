# 認証情報・トークン管理台帳

> 目的：どの認証情報がどこで使われ、どこに保存されているかを一元把握する。
> 値そのもの（パスワード・トークン文字列）はここには書かない → `C:\coo\.env` を参照。
> このファイルには「何がある・どこで使う・状態」だけを記録する。

---

## 1. GitHub パーソナルアクセストークン（PAT）一覧

`https://github.com/settings/tokens` で管理。2026-07-03時点の棚卸し結果。

| トークン名 | スコープ | 有効期限 | 用途 | 関連リポジトリ | .envキー | 状態 |
|---|---|---|---|---|---|---|
| UDSウェブクリエイターボット-2026 | repo | なし | `web-site-hearing-sheet`のPHP（submit.php/submit_meo.php）から`repository_dispatch`を発火、GitHub Actionsでのprivateリポジトリcheckout | zumy8818/web-site-hearing-sheet, zumy8818/uds-web-demos, zumy8818/coo | `GH_ACTIONS_TOKEN` | ✅ 有効（2026-07-03に旧「UDSウェブクリエイターボット」からローテーション済み） |
| replit-sumiyaki-workflow | repo, workflow | 2026-07-11 | 用途未確認（推測：fs-sumiyaki-manager のReplit連携） | 未確認 | 未登録 | ⚠️ 要確認 |
| レプリットスミヤキ | repo | 2026-07-11 | 用途未確認（上記と重複の可能性） | 未確認 | 未登録 | ⚠️ 要確認・重複整理候補 |
| replit-uds | repo | 2026-07-09 | 用途未確認（推測：UDS全般のReplitプロジェクト用） | 未確認 | 未登録 | ⚠️ 要確認 |
| ウェブサイト聴聞会の再推進 | repo | 2026-05-01（期限切れ） | 用途未確認（推測：web-site-hearing-sheet初期構築時） | 未確認 | 未登録 | 🗑 期限切れ・削除候補 |
| GAS-アシスタント | gist, repo | なし | 用途未確認（推測：Google Apps Script連携） | 未確認 | 未登録 | ⚠️ 要確認 |

> ⚠️「要確認」は名称からの推測のみで、実際の用途・使用中リポジトリは未検証。
> 次回このリストを触るときに、実際に使われているか（Replit側の環境変数等）を確認し、不要なものは削除する。

---

## 2. `.env` 管理下の主要認証情報（一覧のみ・値は`.env`参照）

| カテゴリ | .envキー prefix | 用途 | 使用プロジェクト | 最終ローテーション |
|---|---|---|---|---|
| Lolipop MySQL（salesmanage） | `LOLIPOP_DB_*` | メインDB接続 | web-site-hearing-sheet, ubuyama-weather, クライアント管理/php_gateway | 2026-07-04 ✅動作確認済み。**原因判明**：ロリポップのDBパスワード入力欄は16文字までしか保存されない（末尾が黙って切り捨てられる）。20文字で入力→内部的には先頭16文字だけが有効なパスワードになっていたため`Access denied`が続いていた。「パスワード確認」画面に表示される実際の値（16文字）を`.env`・各`config.local.php`・GitHub Secretsに反映して解消。**今後パスワードを再設定する際は16文字以内にすること** |
| Lolipop MySQL（sumiyaki・別DB） | `SUMIYAKI_DB_*` | 炭火焼管理DB接続（mysql327、salesmanageとは別ホスト） | fs-sumiyaki-manager | 2026-07-03（⚠️推測で同じ値に変更。LAA1380072アカウント共通の可能性が高いための対応。**未検証**——fs-sumiyaki-manager本番デプロイ後に動作確認が必要。**2026-07-04追記**：salesmanage側で16文字切り捨てバグが判明したため、こちらも同じ制限を受けている可能性が高い。デプロイ確認時に要注意） |
| Lolipop FTP | `LOLIPOP_FTP_*` | GitHub ActionsからのFTPデプロイ | web-site-hearing-sheet | 2026-07-03（アカウント情報 → FTPパスワードの専用欄から変更。curlでのFTP接続テスト済み ✅） |
| Lolipop PHP APIゲートウェイ | `LOLIPOP_API_*` | クライアント管理DB連携・ubuyama-weather setup用APIキー | クライアント管理/php_gateway, ubuyama-weather | 未ローテーション |
| Lolipop SSH | `LOLIPOP_SSH_*` | SSHトンネル経由MySQL接続 | - | 未ローテーション（他と重複していない値） |
| さくらVPS | `VPS_*` | VPSサーバー管理 | - | ✅完了（2026-07-03）。**さらにSSH鍵認証に切り替え、パスワード認証自体を`/etc/ssh/sshd_config`で無効化済み**（`PasswordAuthentication no` / `KbdInteractiveAuthentication no`）。今後は`~/.ssh/id_ed25519`（コメント "ShopPC"）の鍵でのみログイン可能。`VPS_PASSWORD`は緊急用に記録は残すが、通常は使用しない |
| GitHub Actions | `GH_ACTIONS_TOKEN` | 上記PATと同一 | web-site-hearing-sheet | 2026-07-03 |
| COO管理画面 | `UDS_ADMIN_PASSWORD` | admin.phpログインパスワード | クライアント管理/php_gateway | 未ローテーション |
| Google Maps | `GOOGLE_MAPS_API_KEY` | 地図埋め込み | - | - |
| Claude / Anthropic | `ANTHROPIC_API_KEY` | AI生成全般 | - | - |
| OpenAI | `OPENAI_API_KEY` | 画像生成等 | - | - |
| Facebook | `FB_TAKESHI_*` | クライアントSNS運用（メイン管理者アカウント） | - | 2026-07-03（⚠️`iqxe7130`に記号を足しただけの弱い変更。強いランダム値への再変更を推奨） |
| Lolipopアカウントログイン | `LOLIPOP_LOGIN_PASSWORD` | user.lolipop.jp 管理画面ログイン（FTPとは別の新規発見項目） | - | 2026-07-03（⚠️`iqxe7130`に記号を足しただけの弱い変更。強いランダム値への再変更を推奨） |

> 2026-07-03時点で判明：`LOLIPOP_DB_PASSWORD` / `LOLIPOP_FTP_PASSWORD` / `VPS_PASSWORD` / `FB_TAKESHI_PASSWORD` / `SUMIYAKI_DB_PASSWORD` が同一パスワードを使い回していたため、`web-site-hearing-sheet/lolipop/submit.php`のGit履歴流出をきっかけに個別のパスワードへローテーション対応中。
>
> 同日、同じ平文パスワード直書き問題が以下でも見つかり、`_config.php`（config.local.php/環境変数経由）方式に統一修正済み：
> - `web-site-hearing-sheet/lolipop/{submit.php, notify_demo.php, submit_meo.php}`
> - `プロダクト/ubuyama-weather/lolipop/{fetch_weather.php, api.php, setup.php}`
> - `クライアント管理/php_gateway/{db_gateway.php, admin.php}`
> - `プロダクト/fs-sumiyaki-manager/api/db.php`（別リポジトリ。GitHub Actionsのデプロイ時にSecretsから`config.local.php`を自動生成する方式に変更）
>
> 削除済み：`web-site-hearing-sheet/lolipop/{submit_debug.php, test.php}`（認証なしでDB書き込み可能な旧デバッグファイル）

---

## 3. Bitwarden Secrets Manager（2026-07-03導入）

パスワード使い回し問題をきっかけに、Bitwarden Secrets Manager（無料枠：2ユーザー・無制限シークレット・3プロジェクト・3マシンアカウント）を導入。

| 項目 | 値 |
|---|---|
| 組織名 | ubuyama-digital-service |
| プロジェクト | `coo`（今後増やす場合はここに追記） |
| マシンアカウント | `claude-code`（Claude Codeがこの台帳の代わりに秘密情報を読み書きする用） |
| アクセストークン保存先 | `C:\coo\.env` の `BWS_ACCESS_TOKEN` |
| CLIツール | `bws`（`C:\Users\owner\bin\bws.exe` にインストール済み、PATH通過確認済み） |

**使い方**：`export BWS_ACCESS_TOKEN=...` してから `bws project list` / `bws secret list` / `bws secret create` 等で操作する。

**現状（2026-07-03）**：主要な認証情報28件を`.env`から移行済み（下記キー）。当面`.env`とSecrets Managerを二重管理し、将来的に`.env`を廃止するかは未決定。

移行済みキー：`GH_ACTIONS_TOKEN`, `LOLIPOP_DB_*`（5件）, `LOLIPOP_FTP_*`（4件）, `LOLIPOP_API_*`（2件）, `LOLIPOP_SSH_*`（4件）, `SUMIYAKI_DB_*`（4件）, `UDS_ADMIN_PASSWORD`, `VPS_*`（3件）, `GOOGLE_MAPS_API_KEY`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `FB_TAKESHI_PASSWORD`

未移行（業務データ寄りのため対象外）：請求書・銀行口座情報、`GAS_URL`, `PC_TOKEN`, `FB_TAKESHI_ACCOUNT_URL`/`FB_TAKESHI_LOGIN_EMAIL`, `FB_UDS_PAGE_URL`, `FB_ASOJHFARM_PAGE_URL`

**注意（SSH動作確認時）**：Pythonのparamikoで接続確認する際、`look_for_keys=True`（デフォルト）だと指定したパスワードを無視して自動的に`~/.ssh/`の鍵を使ってしまうことがある。パスワード認証だけを純粋にテストしたい場合は `look_for_keys=False, allow_agent=False` を明示すること。

**注意（Git Bash使用時）**：`/`で始まる値（パス等）を`bws`コマンドの引数に渡すと、Git BashのMSYS自動パス変換で値が壊れることがある（例：`/ubuyama-digital-service.com/uds-api` → `C:/Program Files/Git/ubuyama-digital-service.com/uds-api`）。該当する値を扱う際は `export MSYS_NO_PATHCONV=1` を先に実行してから `bws` を使うこと。

---

## 4. 運用ルール（2026-07-03決定）

1. **新しいトークン・パスワードを発行/発見したら、必ずこの台帳に追記する**（値は書かない。名前・用途・関連プロジェクト・状態のみ）
2. **秘匿性のある値は必ず `C:\coo\.env` に一元管理する**（プロジェクト個別の.envに書きっぱなしにしない。詳細は`CLAUDE.md`14章）
3. **既存のトークン・認証情報は使い回す**。新規タスクのたびに新しいトークンを作らない。まずこの台帳と`.env`を確認し、既存で足りるものは既存を使う
4. コードにトークン・パスワードを直接書かない（`getenv()` や `config.local.php`（Git対象外）経由で読む）
5. 認証情報をローテーションしたら、この台帳の「状態」欄と「最終ローテーション」欄を更新する
